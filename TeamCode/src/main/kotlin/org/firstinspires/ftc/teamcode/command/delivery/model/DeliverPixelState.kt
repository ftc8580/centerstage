package org.firstinspires.ftc.teamcode.command.delivery.model

enum class DeliverPixelState {
    IDLE,
    STARTED,
    RAISING,
    DROPPING_PIXEL,
    ANGLE_UP,
    ANGLE_DOWN,
    LOWERING,
    FINISHED
}